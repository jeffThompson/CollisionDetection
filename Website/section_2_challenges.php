<?php include('includes/header.php'); ?>

<img src="images/challenge.jpg">

<h4>SECTION 2</h4>

<h1>CHALLENGES</h1>

<p>Try these challenge prompts to modify the code we've written:</p>

<p><ol>
	<li>Can you make an object change color gradually as the mouse gets closer? Hint: use <code>map()</code> to convert the distance to a useful range. You can also use <code>lerpColor()</code> to create smooth color transitions.</li>
	<li>As noted, these examples assume the default <code>rectMode(CENTER)</code>. Can you make new versions to work with <code>rectMode(CENTER)</code> instead?</li>
	<li>With <a href="circle-rectangle.php">Circle/Rectangle</a> collision covered, we have enough to build <a href="http://en.wikipedia.org/wiki/Pong">Pong</a>! Can you create a simple version with rectangles as paddles and a circle as the ball?</li>
</ol></p>

<?php include('includes/footer.php'); ?>
